// sugar
const sugaryArray: number[] = [1, 2, 3];

// formal
const formalArray: Array<number> = [4, 5, 6];

// implicit type
const implicitArray = ['hallo', 'vriendjes'];

// implicit mixed types - we'll look at union types later
const mixedArray = [1, '33', 7];
