// using type aliases for documenting
type MonetaryValue = number;

// create a new type alias from a union type,
// e.g. a react component props type:
type Props = InjectedIntlProps & DispatchProps & FilterLabelsProps;

