// strings are basically the same as in JS

const quote = 'single quoted string';
const explicitQuote: string = 'explicitly typed';
const dquote = "double quoted string";
const template = `dank template string with ${quote} and ${dquote}`;

var str: string = 'single';
str = "double";
str = `template string ${str}`;

str = 1;
str = true;
str = NaN;
